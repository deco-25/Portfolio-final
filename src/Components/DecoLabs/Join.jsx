import React from "react";

const Join = () => {
  return (
    <div className="h-screen text-white flex justify-center items-center">
      <div>
        <h1 className="text-8xl max-sm:text-3xl font-extrabold font-ilisarniq max-md:px-6">
          HEY!
        </h1>
        <h1 className="text-8xl max-sm:text-3xl font-extrabold font-ilisarniq max-md:px-6">
          JOIN THE COHORT
        </h1>
        <p className="text-2xl pt-8 font-garet max-md:text-xs max-md:px-6">
          We keep the cohort small so everyone gets the support they need. Lock
          in your spot before it’s gone.
        </p>
      </div>
    </div>
  );
};

export default Join;
